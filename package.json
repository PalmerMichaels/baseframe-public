{
  "name": "baseframe",
  "version": "0.1.0",
  "description": "Synthetic launch baseline frame builder for fictional product readiness snapshots.",
  "license": "MIT",
  "private": false,
  "type": "commonjs",
  "bin": {
    "baseframe": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build --silent && node dist/index.js",
    "test": "npm run build --silent && node test/baseframe.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18"
  }
}
